echo "[*] Running SymlinkDataFolder"

if [ -d $DXHOME/data ] ; then
	rm -r -f $DXHOME/data
fi

mkdir -p "/storage/${KUBE_NAMESPACE}/secondary-data/data"
ln -s "/storage/${KUBE_NAMESPACE}/secondary-data/data" $DXHOME/data
