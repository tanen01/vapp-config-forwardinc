echo "[*] Running SymlinkDataFolder"

if [ -d $DXHOME/data ] ; then
	rm -r -f $DXHOME/data
fi

mkdir -p "/storage/${KUBE_NAMESPACE}/primary-data/data"
ln -s "/storage/${KUBE_NAMESPACE}/primary-data/data" $DXHOME/data
