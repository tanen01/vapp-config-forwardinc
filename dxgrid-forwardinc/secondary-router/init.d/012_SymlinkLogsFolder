echo "[*] Running SymlinkLogsFolder"

if [ -d $DXHOME/logs ] ; then
	rm -r -f $DXHOME/logs
fi

mkdir -p "/storage/${KUBE_NAMESPACE}/secondary-router/logs"
ln -s "/storage/${KUBE_NAMESPACE}/secondary-router/logs" $DXHOME/logs
